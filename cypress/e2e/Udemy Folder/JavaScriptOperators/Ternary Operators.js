
// If a customer has more than 100 poits,
// they are a 'gold' customer, otherwise
// they are a 'silver' customer

let points = 110
let type = points > 100 ? 'Gold' : 'Silver'

console.log(type) // if we change to 90 points it will be a 'Silver' customer
